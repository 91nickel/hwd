# Задания к лекции 13.21 — Форматирование строк

| № | Задание |
|---|---------|
| 13.21.1 | [Слово в кавычках](#задание-132101) |
| 13.21.2 | [Два слова](#задание-132102) |
| 13.21.3 | [Повтор строки](#задание-132103) |
| 13.21.4 | [format с именованными параметрами](#задание-132104) |
| 13.21.5 | [format с позиционными параметрами](#задание-132105) |
| 13.21.6 | [Переменная для f-строки](#задание-132106) |
| 13.21.7 | [Переменные для выражения a + b](#задание-132107) |
| 13.21.8 | [Парсинг двух пар key=value](#задание-132108) |
| 13.21.9 | [Фамилия и инициал](#задание-132109) |
| 13.21.10 | [Центрирование строки](#задание-1321010) |

---

## Задание 13.21.1

Не используя сложение строк, напиши программулечку, которая принимает на вход произвольное слово, и выводит его в кавычках:

```
$ python3 main.py смелость
«смелость»
```

## Ответ

```python
import sys

print(f"«{sys.argv[1].strip()}»")
```

---

## Задание 13.21.2

Не используя сложение строк, напиши программулечку, которая принимает на вход два произвольных слова, и выводит их вот так:

```
$ python3 main.py смелость храбрость
первое слово: смелость
второе слово: храбрость
```

Используй `print` один раз и не используй сложение строк.

## Ответ

```python
import sys

print(f"первое слово: {sys.argv[1].strip()}\nвторое слово: {sys.argv[2].strip()}")
```

---

## Задание 13.21.3

Напиши программулечку, которая принимает на вход строку и положительное целое число и печатает строку, повторённую указанное количество раз:

```
$ python3 main.py "кто здесь?" 2
кто здесь?
кто здесь?

$ python3 main.py "что происходит?!" 4
что происходит?!
что происходит?!
что происходит?!
что происходит?!
```

## Ответ

```python
import sys

print((f"{sys.argv[1].strip()}\n" * int(sys.argv[2])).strip())
```

---

## Задание 13.21.4

Код, предшествующий твоему:

```python
import sys

message_template = """Здравствуйте, {name}!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, {name}!"""
user_name = sys.argv[1]
```

Напиши одну строку, чтобы вся программа работала вот так:

```
$ python3 main.py "Пётр Иванович"
Здравствуйте, Пётр Иванович!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, Пётр Иванович!

$ python3 main.py "Санёк"
Здравствуйте, Санёк!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, Санёк!
```

Обрати внимание — задание `message_template` и `user_name` не повторяй. Это код, который есть где-то выше твоего кода, то есть у тебя в твоём коде уже есть эти переменные, просто используй их.

## Ответ

```python
print(message_template.format(name=user_name))
```

---

## Задание 13.21.5

Код, предшествующий твоему:

```python
import sys

message_template = """Здравствуйте, {}!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, {}!"""
user_name = sys.argv[1]
```

Напиши одну строку, чтобы вся программа работала вот так:

```
$ python3 main.py "Пётр Иванович"
Здравствуйте, Пётр Иванович!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, Пётр Иванович!

$ python3 main.py "Санёк"
Здравствуйте, Санёк!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, Санёк!
```

Обрати внимание — задание `message_template` и `user_name` не повторяй. Это код, который есть где-то выше твоего кода, то есть у тебя в твоём коде уже есть эти переменные, просто используй их.

## Ответ

```python
print(message_template.format(user_name, user_name))
```

---

## Задание 13.21.6

Код, идущий сразу после твоего кода:

```python
print(f"""Здравствуйте, {name}!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, {name}!""")
```

Напиши свой код, чтобы вся программа работала вот так:

```
$ python3 main.py "Пётр Иванович"
Здравствуйте, Пётр Иванович!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, Пётр Иванович!

$ python3 main.py "Санёк"
Здравствуйте, Санёк!

Вынуждены Вам сообщить, что Вы большой молодец. Да. Так уж вышло.

До свидания, Санёк!
```

Обрати внимание — не повторяй код с `print`, указанный выше. Это код, который идёт после твоего кода, просто положись на это.

## Ответ

```python
import sys

name = sys.argv[1]
```

---

## Задание 13.21.7

Код, идущий сразу после твоего кода:

```python
print(f"{a + b = }")
```

Напиши свой код, чтобы вся программа работала вот так:

```
$ python3 main.py 10 20
a + b = 30

$ python3 main.py 50 6
a + b = 56
```

Обрати внимание — не повторяй код с `print`, приведённый выше.

## Ответ

```python
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
```

---

## Задание 13.21.8

Напиши программулечку, которая работает вот так:

```
$ python3 main.py "x=10,y=20"
x = 10; y = 20

$ python3 main.py "tmp=23231,another=5414"
tmp = 23231; another = 5414

$ python3 main.py "tmp=-23231,another=5414"
tmp = -23231; another = 5414
```

Известно, что во входной строке может быть строго два знака `=`.

Используй f-строку. Не используй тут списки, кортежи, множества, словари, регулярные выражения (мы о них будем говорить дальше в этой главе курса).

## Ответ

```python
import sys

arg = sys.argv[1]

first_eq_position = arg.find("=")
second_eq_position = arg.rfind("=")
comma_position = arg.find(",")

first_key = arg[:first_eq_position]
second_key = arg[comma_position + 1:second_eq_position]

first_value = arg[first_eq_position + 1:comma_position]
second_value = arg[second_eq_position + 1:]

print(f"{first_key} = {first_value}; {second_key} = {second_value}")
```

Да, тут пришлось очень внимательно в математику, ясное дело :) Во все эти `+ 1` и подобное, внимательно с индексами.

Если бы можно было пользоваться списками (о которых дальше будем говорить), то было бы легко:

```python
import sys

arg = sys.argv[1]

first_param, second_param = arg.split(",")
first_key, first_value = first_param.split("=")
second_key, second_value = second_param.split("=")

print(f"{first_key} = {first_value}; {second_key} = {second_value}")
```

А если бы можно было использовать регулярные выражения (тоже дальше о них поговорим):

```python
import re
import sys

arg = sys.argv[1]
pattern = r"^([^=]+)=(-?\d+),([^=]+)=(-?\d+)$"
match = re.fullmatch(pattern, arg)
first_key, first_value, second_key, second_value = match.groups()
print(f"{first_key} = {first_value}; {second_key} = {second_value}")
```

---

## Задание 13.21.9

Напиши программулечку, которая работает вот так:

```
$ python3 main.py "иван петров"
Петров И.

$ python3 main.py "алексей Мусечкин"
Мусечкин А.

$ python3 main.py "василий смирнов"
Смирнов В.

$ python3 main.py "   василий   смирнов  "
Смирнов В.
```

Известно, что и имя, и фамилия строго состоят из одного слова и разделены одним или несколькими пробелами.

Используй f-строку. Не используй тут списки, кортежи, множества, словари, регулярные выражения (мы о них будем говорить дальше в этой главе курса).

## Ответ

```python
import sys

fullname = sys.argv[1].strip()

first_space_pos = fullname.find(' ')

name = fullname[:first_space_pos].capitalize()
last_name = fullname[first_space_pos + 1:].strip().capitalize()

print(f"{last_name} {name[0]}.")
```

---

## Задание 13.21.10

Напиши программулечку, которая принимает на вход три параметра — начальную строку, число и один символ:

```
$ python3 main.py "привет" 20 "="
=======привет=======

$ python3 main.py "привет" 20 "."
.......привет.......

$ python3 main.py "XXX" 2 "."
XXX

$ python3 main.py "XXX" 3 "."
XXX

$ python3 main.py "XXX" 4 "."
XXX.

$ python3 main.py "XXX" 5 "."
.XXX.

$ python3 main.py "XXX" 6 "."
.XXX..

$ python3 main.py "XXXX" 4 "."
XXXX

$ python3 main.py "XXXX" 5 "."
.XXXX

$ python3 main.py "XXXX" 6 "."
.XXXX.

$ python3 main.py "XXXX" 7 "."
..XXXX.
```

## Ответ

```python
import math
import sys

word = sys.argv[1].strip()
length = int(sys.argv[2])
sep = sys.argv[3].strip()

if length <= len(word):
    output = word
else:
    if len(word) % 2 == 0:
        left_len = math.ceil((length - len(word)) / 2)
        right_len = length - len(word) - left_len
    else:
        right_len = math.ceil((length - len(word)) / 2)
        left_len = length - len(word) - right_len

    output = sep * left_len + word + sep * right_len

print(output)
```

### Сообщение от преподавателя

У строк есть встроенная функция `center`:

```python
import sys

print(sys.argv[1].center(int(sys.argv[2]), sys.argv[3]))
```

Для продакшн-кода, конечно, стоит побить по переменным, чтобы было понятно, что тут происходит:

```python
import sys

input_string = sys.argv[1]
required_length = int(sys.argv[2])
fill_char = sys.argv[3]
centered = input_string.center(required_length, fill_char)
print(centered)
```

Конечно, как и всегда, надо обработать ошибки — не показываю это тут, потому что пока не прошли работу с исключениями и ветвление логики. Что тут надо проверить? Надо проверить, что переданы все необходимые аргументы. Что второй аргумент целое число. Что последний аргумент это один символ.
